namespace Mojica.Api.Tests.Ports;

public sealed class GeneratedImageDataSmallTests
{
    [Fact(Skip = "TODO: Implement after the GeneratedImageData contract exists.")]
    public void GeneratedImageData_Create_WhenResultIsValid_PreservesContentAndMediaType()
    {
        // ID: PORT-DATA-01
        // Source: docs/v1/api/ports.md §2 and §4.
        // Given: binary image content and its image media type
        // When: GeneratedImageData is created from the external generation result
        // Then: the result exposes the same image content and media type without adding a download filename
        // Blocked by: feature/add-image-generation-port must define GeneratedImageData
        // Priority: High
    }
}
