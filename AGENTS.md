# Repository Agent Instructions

## Required skill

Before starting work, read and apply:

- `work-session-log` — record the exact user prompts, decisions, progress, verification, and resume checkpoint in `docs/work-logs/`.

The work log exists to provide evidence for deciding whether recurring work should become a skill or an AI configuration change. Update it at each meaningful scope change and before ending a session.
